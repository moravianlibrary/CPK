<? foreach ($this->tab->getLibraries() as $libname => $id): ?>
  <p><a href="<?=$this->recordLink()->related(array('type' => 'bib', 'value' => $id))?>"><?=$this->transEsc(strtoupper($libname))?></a></p>
<? endforeach; ?>